# Render deployment configuration
services:
  - type: web
    name: scanmybody-api
    runtime: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
